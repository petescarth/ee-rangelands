from google.appengine.ext import vendor

# Add any libraries installed in the "lib" folder.
# Make the lib directory and install them first with:
# pip install -t lib -r requirements.txt
vendor.add('lib')

